# candidates

Candidate component implementations live here when generation or runtime work needs a new surface that has not been promoted to `src/components/`.

Candidates are internal status entries. They must be exposed through the single public `@cx/components/catalog` vocabulary after review, not through candidate-specific package subpaths.
