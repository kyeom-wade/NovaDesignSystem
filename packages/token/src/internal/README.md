# Token Internals

This directory is reserved for token import, normalization, validation, and generation scripts.

Nothing in this directory is part of the `@cx/tokens` public package surface. Consumers must use only the exports declared in `packages/token/package.json`.
